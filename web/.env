# Warning: Do not add secrets (API keys and similar) to this file, as it source controlled!
# Use `.env.local` for any secrets, and ensure it is not added to source control

POSTGRES_PRISMA_URL=postgresql://user:password@localhost:5432/database
EMAIL_SERVER=smtp://admin:password@localhost:1025
EMAIL_FROM=noreply@myapp.com
NEXTAUTH_URL="https://${CODESPACE_NAME}-3000.${GITHUB_CODESPACES_PORT_FORWARDING_DOMAIN}/"
S3_ENDPOINT="http://localhost:9090"
ACCESS_KEY_ID="dev"
SECRET_ACCESS_KEY="dev"